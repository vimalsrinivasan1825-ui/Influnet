// Moved to packages/core — shared with apps/mobile and with PhoneSchema in
// validators.ts, which needs the same rule signup and profile updates use.
// Re-exported for existing imports (@/lib/phone).
export * from '@influnet/core';
